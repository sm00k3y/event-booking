import React from "react";
import Spinner from "../components/Spinner/Spinner";

const FoxyPage = () => {
  return (
    <React.Fragment>
      <Spinner />
    </React.Fragment>
  );
};

export default FoxyPage;
